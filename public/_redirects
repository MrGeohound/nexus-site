# SPA fallback (Cloudflare Pages / Netlify)
# Garante que arquivos de assets ausentes não retornem HTML (evita erro de MIME type no browser)
/assets/*  /assets/:splat  404
/*         /index.html     200
